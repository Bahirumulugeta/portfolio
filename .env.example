# Copy to .env.local for local development.
# Also add these in Vercel → Project → Settings → Environment Variables.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=bahirumulugeta1@gmail.com
SMTP_PASS=
SMTP_FROM=bahirumulugeta1@gmail.com
NOTIFY_EMAIL=bahirumulugeta1@gmail.com
